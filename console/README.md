# OneClick Console

Bootstrap inside this folder:

    npx create-next-app@latest . --ts --tailwind --app --eslint

Routes: `/` console, `/metrics`, `/architecture` (Screen Graph explorer).
Set `NEXT_PUBLIC_API_URL` to the API base URL.
